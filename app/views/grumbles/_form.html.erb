<%= form_for(@grumble) do |f| %>
  <% if @grumble.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grumble.errors.count, "error") %> prohibited this grumble from being saved:</h2>

      <ul>
      <% @grumble.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :authorName %><br>
    <%= f.text_field :authorName %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :photoUrl %><br>
    <%= f.text_area :photoUrl %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
